{
  "name": "policyready-stripe",
  "version": "1.0.0",
  "private": true,
  "description": "Stripe checkout + secure download delivery for PolicyReady.org manuals",
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
